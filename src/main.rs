//! CLI entry point
use crypto_app::cli::Args;
use clap::Parser;

fn main() {
    let args = Args::parse();
    // Handle encryption/decryption based on args
    // Use core modules to process files/dirs
}